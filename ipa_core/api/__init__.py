"""API (CLI/HTTP) stubs.

Estado: Implementación pendiente (definir comandos y endpoints).

TODO
----
- CLI: exponer un comando mínimo `transcribe` que reciba `--audio` y `--lang`.
- HTTP: exponer `GET /health` y `POST /v1/compare`.
- Mantener las firmas simples y consistentes con los tipos en `ipa_core.types`.
"""
